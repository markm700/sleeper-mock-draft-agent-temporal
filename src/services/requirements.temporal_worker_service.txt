httpx[standard]
pytest
pytest-asyncio
slugify
temporalio==1.21.1

# PostgreSQL
SQLAlchemy>=2.0
psycopg2-binary

# PyTorch -- CUDA 12.4 wheels are served from the index below.
# On CPU-only hosts pip automatically falls back to the standard PyPI CPU wheel.
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.10.0            # Core tensor library; CUDA variant selected via index above
torchvision>=0.17.0     # Vision utilities (transforms, datasets)
torchaudio>=2.2.0       # Audio ops (included for full PyTorch ecosystem compatibility)

# Numerical / ML utilities
scikit-learn>=1.3.0     # Model evaluation metrics (accuracy, AUC, precision, recall)
numpy>=1.24.0           # Numerical operations

# GPU training utilities
accelerate>=0.27.0      # Device-agnostic training loops (Hugging Face Accelerate)
tensorboard>=2.15.0     # Training run monitoring and visualization
tqdm>=4.66.0            # Progress bars for training loops